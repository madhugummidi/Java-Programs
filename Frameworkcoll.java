import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;

public class Collections {
    
    /*  Collections in Java refers to a framework that provides a classes and interfaces 
        to store and manipulate group of objects and it is defined in java.util package

        Key Interfaces in java are:

        List  -----> ArrayList, LinkedList
        It is ordered collection it maintain insertion order and allows duplicates
        ArrayList   -  It is used for random access allows null
        LinkedList  -  It is used for frequent add/remove allows null

        Set   -----> HashSet, LinkedHashSet, TreeSet
        It is unordered collection and not allow duplicates
        HashSet        -  It is unordered allows one null
        LinkedHashSet  -  It follows insertion order
        TreeSet        -  It is unordered does not allow null

        Queue -----> PriorityQueue, ArrayDeque
        It follows FIFO(First In First Out)
        PriorityQueue   -  It follows Natural Order it retrives based on priority high/low
        ArrayDeque      -  It follows FIFO

        Map   -----> HashMap, TreeMap
        It stores values in key,value pairs and not allow duplicate keys can have values duplicate
        HashMap     -   Unordered allows one null key
        TreeMap     -   Sorted by key does not allow null keys
    */

    /*
    //List
    public static void main(String[] args) {
        
        List<String> arrayList = new ArrayList<>();
        List<String> linkedList = new LinkedList<>();

        String[] fruits = {"Apple","Banana","Carrot"};

        //Adding elements to list
        for(String fruit : fruits)
        {
            arrayList.add(fruit);
        }

        for(String fruit : fruits)
        {
            linkedList.add(fruit);
        }

        //Retriving elements
        for(String fruit : arrayList)
        {
            System.out.println(fruit);
        }
        System.out.println("------------");
        for(String fruit : linkedList)
        {
            System.out.println(fruit);
        }

    }
    */

    
    // Set
    public static void main(String[] args) {
        
        
    }

    


    

}
